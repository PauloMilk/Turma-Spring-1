CREATE TABLE TB_ALUNO(
                         CD_ALUNO varchar(255),
                         nm_aluno varchar(255) not null,
                         constraint PK_ALUNO
                             PRIMARY KEY (CD_ALUNO)
);
